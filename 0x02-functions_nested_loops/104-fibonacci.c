#include <stdio.h>

/**
 * main - fibonacci <3
 *
 * Purpose - no hardcode
 *
 * Return: (Success)
 */

int main(void)
{
	unsigned long int i;
	unsigned long int bef = 1;
	unsigned long int atf = 2;
	unsigned long int bef = 1;
        unsigned long int atf = 2;
        unsigned long int T = 1000000000;
        unsigned long int bef1;
        unsigned long int bef2;
        unsigned long int atf1;
        unsigned long int atf2;

        printf("%lu", bef);

        for (i = 1; i < 91; i++)
	{
	       printf("%lu", atf;
	       atf += bef;
               bef = atf - bef;
        }
        bef1 = (bef / 1);
        bef2 = (bef / 2);
        atf1 = (atf / 1);
        atf2 = (atf / 2);
        for (i = 92; i <99; ++i)
        {
               printf("%lu", atf1 + (atf2 / 1);
	       printf("%lu", atf2 % 1);
               atf1 = atf1 + bef1;
               bef1 = atf1 - bef1;
               atf2 = atf2 + bef2;
               bef2 = atf2 - bef2;
	}
	printf("/n");
	return (0)
}
